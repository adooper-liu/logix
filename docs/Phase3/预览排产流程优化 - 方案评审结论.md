# 预览排产流程优化 - 方案评审结论与实施清单

**评审日期**: 2026-03-27  
**评审目标**: 验证选项 B（保存时使用预览数据）的可行性  
**评审范围**: 后端 API、前端组件复用、技术风险评估

---

## 一、方案验证结论

### ✅ 1. 技术可行性验证

#### 1.1 后端 API 改造可行性

**当前实现** (scheduling.controller.ts:157-200):

```typescript
confirmSchedule = async (req: Request, res: Response): Promise<void> => {
  try {
    const { containerNumbers } = req.body; // 👈 只接收 containerNumbers

    // 重新执行排产（正式模式，dryRun=false）
    const result = await intelligentSchedulingService.batchSchedule({
      containerNumbers,
      dryRun: false,
    });

    res.json({ success: result.success, savedCount: result.successCount });
  } catch (error) {
    // 错误处理
  }
};
```

**改造方案**:

```typescript
confirmSchedule = async (req: Request, res: Response): Promise<void> => {
  try {
    const { containerNumbers, previewResults } = req.body;  // 👈 新增参数

    if (previewResults && Array.isArray(previewResults)) {
      // ✅ 使用预览数据（快速路径）
      result = await this.savePreviewResults(previewResults);
    } else {
      // 👈 向后兼容：重新计算（慢速路径）
      result = await intelligentSchedulingService.batchSchedule({
        containerNumbers,
        dryRun: false
      });
    }
  }
}
```

**✅ 结论**: **完全可行**

- 后端接口可以扩展接收 `previewResults` 参数
- 保持向后兼容（不传 previewResults 时重新计算）
- 需要新增 `savePreviewResults` 方法

---

#### 1.2 前端调用改造可行性

**当前实现** (SchedulingVisual.vue:1553-1592):

```typescript
const handleConfirmSave = async () => {
  const result = await containerService.confirmSchedule({
    containerNumbers: selectedPreviewContainers.value,
    // ❌ 没有传 previewResults
  });
};
```

**改造方案**:

```typescript
const handleConfirmSave = async () => {
  const previewDataToSave = previewResults.value.filter((result: any) => selectedPreviewContainers.value.includes(result.containerNumber));

  const result = await containerService.confirmSchedule({
    containerNumbers: selectedPreviewContainers.value,
    previewResults: previewDataToSave, // ✅ 新增：传回预览数据
  });
};
```

**✅ 结论**: **完全可行**

- 前端已有 `previewResults` 和 `selectedPreviewContainers` 数据
- 只需修改 API 调用，增加参数传递
- 无需修改 UI 逻辑

---

### ✅ 2. 组件复用分析验证

#### 2.1 实际使用的组件

| 组件名                            | 实际使用情况           | 复用价值 | 验证结果              |
| --------------------------------- | ---------------------- | -------- | --------------------- |
| **OptimizationResultCard.vue**    | ✅ 正在使用 (Line 736) | ⭐⭐⭐   | 用于单柜优化结果展示  |
| **DesignatedWarehouseDialog.vue** | ✅ 正在使用 (Line 721) | ⭐⭐     | 用于手工指定仓库      |
| **内联 el-table**                 | ✅ 正在使用 (Line 364) | ⭐⭐⭐   | 用于预览/正式结果展示 |

#### 2.2 未使用的组件（用户分析正确）

| 组件名                        | 文件位置    | 验证结果      | 建议           |
| ----------------------------- | ----------- | ------------- | -------------- |
| **CostOptimizationPanel.vue** | components/ | ✅ 确实未使用 | 移除或重构集成 |
| **SchedulingResultCard.vue**  | components/ | ✅ 确实未使用 | 可考虑复用     |
| **UnloadOptionSelector.vue**  | components/ | ✅ 确实未使用 | 可考虑复用     |

#### 2.3 组件复用机会

**正确的复用策略**:

1. **OptimizationResultCard.vue** - 已经复用 ✅
   - 用途 1: SchedulingVisual.vue (Line 736) - 单柜优化
   - 用途 2: SchedulingPreviewModal.vue (Line 452) - 批量优化
   - **状态**: 复用良好，无需改动

2. **内联表格 vs SchedulingResultCard** - 建议保持现状 ✅
   - 当前：使用内联 `<el-table>` (更灵活)
   - 备选：使用 `SchedulingResultCard` 组件
   - **建议**: 当前内联方式更适合复杂场景，无需强行复用

3. **CostBreakdownDisplay** - 需验证是否存在
   - 搜索结果：文件存在
   - 实际使用：**未在 SchedulingVisual.vue 中找到使用**
   - 当前做法：使用 `<el-tree>` 内联实现费用展示 (Line 484)
   - **建议**: 如果后续需要统一费用展示，可以复用

**✅ 结论**:

- 用户分析的"组件未使用"情况**基本正确**
- 但**不需要强行复用所有组件**
- 当前内联实现更灵活，适合复杂业务场景

---

### ✅ 3. 关键 Bug 发现

#### Bug #1: 优化按钮无响应

**问题代码** (SchedulingVisual.vue:1813-1818):

```typescript
const handleAcceptOptimization = (alternative: any) => {
  console.log("[SchedulingVisual] 接受优化方案:", alternative);
  // TODO: 实际保存优化结果到数据库  ❌ 只有注释，没有实现
  ElMessage.success("优化方案已应用");
  showOptimizationDialog.value = false;
  // TODO: 刷新列表或更新对应行的数据  ❌ 没有实际更新
};
```

**影响**:

- 用户点击"接受优化方案"按钮后，只显示提示，**没有实际保存**
- 用户体验断裂：看到优化建议，但无法应用

**修复方案**:

```typescript
const handleAcceptOptimization = async (alternative: any) => {
  try {
    // 1. 获取当前货柜的完整预览数据
    const currentContainer = previewResults.value.find((r: any) => r.containerNumber === currentOptimizationContainer.value);

    if (!currentContainer) {
      ElMessage.error("未找到货柜数据");
      return;
    }

    // 2. 更新为优化后的方案
    const optimizedData = {
      ...currentContainer,
      plannedData: {
        ...currentContainer.plannedData,
        plannedPickupDate: alternative.pickupDate,
        unloadMode: alternative.strategy,
        // 其他字段也需要更新
      },
      estimatedCosts: alternative.breakdown,
    };

    // 3. 调用后端保存优化结果
    saving.value = true;
    const result = await containerService.confirmSchedule({
      containerNumbers: [currentOptimizationContainer.value],
      previewResults: [optimizedData], // 传回优化后的数据
    });

    if (result.success) {
      ElMessage.success("优化方案已保存");
      showOptimizationDialog.value = false;

      // 4. 刷新列表
      await loadOverview();
      await executeScheduling(false); // 重新预览
    } else {
      ElMessage.error("保存失败：" + result.message);
    }
  } catch (error: any) {
    ElMessage.error("保存失败：" + error.message);
  } finally {
    saving.value = false;
  }
};
```

**优先级**: **P0 (最高)**

- 影响核心功能
- 用户体验严重受损
- 修复成本低 (0.5 天)

---

## 二、风险评估

### ⚠️ 风险 1: 前端数据被篡改

**风险描述**: 用户可以修改浏览器中的 JavaScript 代码，篡改 previewResults 数据

**可能性**: 低（需要一定技术能力）  
**影响程度**: 高（可能导致数据不一致）

**缓解措施**:

```typescript
// 后端必须验证数据完整性
private validatePreviewResult(preview: any): boolean {
  // 1. 必填字段检查
  if (!preview.containerNumber || !preview.plannedData) {
    return false
  }

  // 2. 日期格式验证
  const { plannedData } = preview
  if (!plannedData.plannedPickupDate ||
      !plannedData.plannedUnloadDate ||
      !plannedData.plannedReturnDate) {
    return false
  }

  // 3. 业务规则验证
  if (new Date(plannedData.plannedPickupDate) >
      new Date(plannedData.plannedUnloadDate)) {
    return false  // 提柜日期不能晚于卸柜日期
  }

  return true
}
```

**✅ 结论**: 风险可控，通过后端验证可防止大部分篡改

---

### ⚠️ 风险 2: 资源超卖

**风险描述**: 多个用户同时保存预览结果，可能导致仓库/车队资源超卖

**可能性**: 中（并发场景下）  
**影响程度**: 高（资源冲突）

**缓解措施**:

```typescript
// 保存前检查资源可用性
private async checkResourceAvailability(preview: any): Promise<boolean> {
  const { plannedData } = preview

  // 检查仓库档期
  const warehouseOccupancy = await this.warehouseOccupancyRepo.findOne({
    where: {
      warehouseCode: plannedData.warehouseId,
      date: dateTimeUtils.formatDateOnly(new Date(plannedData.plannedUnloadDate))
    }
  })

  const warehouse = await this.warehouseRepo.findOne({
    where: { warehouseCode: plannedData.warehouseId }
  })

  if (warehouse && warehouseOccupancy) {
    const currentCapacity = warehouseOccupancy.occupiedCapacity || 0
    if (currentCapacity >= warehouse.dailyCapacity) {
      return false  // 仓库已满
    }
  }

  // 如果是 Drop off 模式，检查车队还箱档期
  if (plannedData.unloadMode === 'Drop off') {
    // 类似逻辑检查车队资源
  }

  return true  // 资源可用
}
```

**✅ 结论**: 风险可控，通过资源检查可避免超卖

---

### ⚠️ 风险 3: 向后不兼容

**风险描述**: 旧版前端不传 previewResults，可能导致后端报错

**可能性**: 低  
**影响程度**: 中

**缓解措施**:

```typescript
// 向后兼容设计
if (previewResults && Array.isArray(previewResults)) {
  // 新路径：使用预览数据
  result = await this.savePreviewResults(previewResults);
} else {
  // 旧路径：重新计算（兼容旧版前端）
  result = await intelligentSchedulingService.batchSchedule({
    containerNumbers,
    dryRun: false,
  });
}
```

**✅ 结论**: 风险极低，通过条件判断可完美兼容

---

## 三、实施清单

### Phase 0: 紧急修复（P0 - 0.5 天）

#### 任务 1: 修复"接受优化方案"按钮无响应

- [ ] **修改 handleAcceptOptimization 方法**
  - 文件：`frontend/src/views/scheduling/SchedulingVisual.vue`
  - 行号：1813-1818
  - 改动：添加实际保存逻辑
- [ ] **测试验证**
  - 点击"优化"按钮
  - 选择最优方案
  - 点击"接受优化方案"
  - 确认数据已保存到数据库
  - 确认列表已刷新

---

### Phase 1: 后端改造（P1 - 1 天）

#### 任务 1: 添加 savePreviewResults 方法

- [ ] **文件**: `backend/src/controllers/scheduling.controller.ts`
- [ ] **位置**: confirmSchedule 方法下方

```typescript
/**
 * 保存预览结果
 */
private async savePreviewResults(previewResults: any[]): Promise<{
  success: boolean;
  successCount: number;
  results: any[];
}> {
  const results = [];
  let successCount = 0;

  for (const preview of previewResults) {
    try {
      // 1. 验证数据完整性
      if (!this.validatePreviewResult(preview)) {
        throw new Error('预览数据格式不正确');
      }

      // 2. 验证资源可用性
      const resourceAvailable = await this.checkResourceAvailability(preview);
      if (!resourceAvailable) {
        throw new Error('仓库或车队资源不足');
      }

      // 3. 查找货柜
      const container = await this.containerRepo.findOne({
        where: { containerNumber: preview.containerNumber }
      });

      if (!container) {
        throw new Error('货柜不存在');
      }

      // 4. 更新状态
      container.scheduleStatus = 'issued';
      await this.containerRepo.save(container);

      // 5. 保存计划日期
      const destPo = container.portOperations?.find(
        (po: any) => po.portType === 'destination'
      );

      if (destPo && preview.plannedData) {
        destPo.plannedPickupDate = new Date(preview.plannedData.plannedPickupDate);
        destPo.plannedUnloadDate = new Date(preview.plannedData.plannedUnloadDate);
        destPo.plannedReturnDate = new Date(preview.plannedData.plannedReturnDate);
        await this.portOperationRepo.save(destPo);
      }

      // 6. 占用资源
      if (preview.plannedData) {
        await this.incrementWarehouseOccupancy(
          preview.plannedData.warehouseId,
          new Date(preview.plannedData.plannedUnloadDate)
        );

        if (preview.plannedData.unloadMode === 'Drop off') {
          await this.decrementFleetReturnOccupancy(
            preview.plannedData.truckingCompanyId,
            new Date(preview.plannedData.plannedReturnDate)
          );
        }
      }

      results.push({
        containerNumber: preview.containerNumber,
        success: true,
        message: '保存成功'
      });
      successCount++;

    } catch (error: any) {
      results.push({
        containerNumber: preview.containerNumber,
        success: false,
        message: error.message
      });
    }
  }

  return { success: successCount > 0, successCount, results };
}
```

#### 任务 2: 添加 validatePreviewResult 方法

- [ ] **文件**: `backend/src/controllers/scheduling.controller.ts`
- [ ] **位置**: savePreviewResults 方法下方

```typescript
/**
 * 验证预览数据完整性
 */
private validatePreviewResult(preview: any): boolean {
  if (!preview.containerNumber || !preview.plannedData) {
    return false;
  }

  const { plannedData } = preview;

  if (!plannedData.plannedPickupDate ||
      !plannedData.plannedUnloadDate ||
      !plannedData.plannedReturnDate) {
    return false;
  }

  if (!plannedData.warehouseId || !plannedData.truckingCompanyId) {
    return false;
  }

  return true;
}
```

#### 任务 3: 添加 checkResourceAvailability 方法

- [ ] **文件**: `backend/src/controllers/scheduling.controller.ts`
- [ ] **位置**: validatePreviewResult 方法下方

```typescript
/**
 * 检查资源可用性（防止超卖）
 */
private async checkResourceAvailability(preview: any): Promise<boolean> {
  try {
    const { plannedData } = preview;

    // 检查仓库档期
    const warehouseOccupancy = await this.warehouseOccupancyRepo.findOne({
      where: {
        warehouseCode: plannedData.warehouseId,
        date: dateTimeUtils.formatDateOnly(new Date(plannedData.plannedUnloadDate))
      }
    });

    const warehouse = await this.warehouseRepo.findOne({
      where: { warehouseCode: plannedData.warehouseId }
    });

    if (warehouse && warehouseOccupancy) {
      const currentCapacity = warehouseOccupancy.occupiedCapacity || 0;
      if (currentCapacity >= warehouse.dailyCapacity) {
        return false;
      }
    }

    // 检查车队还箱档期（如果是 Drop off）
    if (plannedData.unloadMode === 'Drop off') {
      const truckingOccupancy = await this.truckingOccupancyRepo.findOne({
        where: {
          truckingCompanyId: plannedData.truckingCompanyId,
          date: dateTimeUtils.formatDateOnly(new Date(plannedData.plannedReturnDate)),
          portCode: preview.destinationPort
        }
      });

      if (truckingOccupancy && truckingOccupancy.isFullyOccupied) {
        return false;
      }
    }

    return true;
  } catch (error) {
    logger.warn('[Scheduling] Resource availability check failed:', error);
    return true; // 检查失败时默认允许
  }
}
```

#### 任务 4: 修改 confirmSchedule 方法

- [ ] **文件**: `backend/src/controllers/scheduling.controller.ts`
- [ ] **位置**: Line 157-200
- [ ] **改动**:

```typescript
confirmSchedule = async (req: Request, res: Response): Promise<void> => {
  try {
    const { containerNumbers, previewResults } = req.body; // ✅ 新增参数

    // 验证参数
    if (!Array.isArray(containerNumbers) || containerNumbers.length === 0) {
      res.status(400).json({
        success: false,
        message: "containerNumbers 不能为空",
      });
      return;
    }

    logger.info(`[Scheduling] Confirm schedule request:`, {
      containerNumbers,
      hasPreviewResults: !!previewResults,
    });

    let result;

    // ✅ 如果有预览结果，直接保存
    if (previewResults && Array.isArray(previewResults)) {
      result = await this.savePreviewResults(previewResults);
    } else {
      // 否则重新计算（向后兼容）
      result = await intelligentSchedulingService.batchSchedule({
        containerNumbers,
        dryRun: false,
      });
    }

    logger.info(`[Scheduling] Confirmed ${result.successCount}/${containerNumbers.length} containers`);

    res.json({
      success: result.success,
      savedCount: result.successCount,
      total: containerNumbers.length,
      results: result.results.map((r) => ({
        containerNumber: r.containerNumber,
        success: r.success,
        message: r.message,
      })),
    });
  } catch (error: any) {
    logger.error("[Scheduling] confirmSchedule error:", error);
    res.status(500).json({
      success: false,
      message: error.message || "确认保存失败",
      savedCount: 0,
    });
  }
};
```

#### 任务 5: 编写单元测试

- [ ] **文件**: `backend/test/scheduling.controller.test.ts`
- [ ] **测试用例**:
  - [ ] 有 previewResults 时的保存逻辑
  - [ ] 没有 previewResults 时的重新计算逻辑
  - [ ] 数据验证逻辑（无效数据应拒绝）
  - [ ] 资源可用性检查（资源不足应拒绝）

---

### Phase 2: 前端改造（P1 - 0.5 天）

#### 任务 1: 修改 handleConfirmSave 方法

- [ ] **文件**: `frontend/src/views/scheduling/SchedulingVisual.vue`
- [ ] **位置**: Line 1553-1592
- [ ] **改动**:

```typescript
const handleConfirmSave = async () => {
  if (selectedPreviewContainers.value.length === 0) {
    ElMessage.warning("请至少选择一个货柜");
    return;
  }

  try {
    saving.value = true;
    addLog(`正在保存 ${selectedPreviewContainers.value.length} 个货柜的排产结果...`, "info");

    // ✅ 关键改进：传回完整的预览结果
    const previewDataToSave = previewResults.value.filter((result: any) => selectedPreviewContainers.value.includes(result.containerNumber));

    // 调用 confirm 接口（带预览数据）
    const result = await containerService.confirmSchedule({
      containerNumbers: selectedPreviewContainers.value,
      previewResults: previewDataToSave, // ✅ 新增：传回预览数据
    });

    if (result.success) {
      ElMessage.success(`成功保存 ${result.savedCount} 个货柜`);
      addLog(`确认保存完成：成功 ${result.savedCount} 个`, "success");

      // 清空预览状态
      isPreviewMode.value = false;
      previewResults.value = [];
      selectedPreviewContainers.value = [];

      // 刷新概览数据
      await loadOverview();

      // 触发完成事件
      emit("complete", result);
    } else {
      ElMessage.error("保存失败：" + (result as any).message);
      addLog("保存失败：" + (result as any).message, "error");
    }
  } catch (error: any) {
    ElMessage.error("保存失败：" + (error.message || "未知错误"));
    addLog("保存失败：" + error.message, "error");
  } finally {
    saving.value = false;
  }
};
```

#### 任务 2: 修改 handleAcceptOptimization 方法（已在 Phase 0 完成）

- [ ] **状态**: Phase 0 已完成
- [ ] **验证**: 确保优化方案能正确保存

---

### Phase 3: 测试与验证（P2 - 1 天）

#### 任务 1: 功能测试

- [ ] **预览排产正常**
  - 点击"预览排产"
  - 查看预览结果
  - 确认费用明细正确

- [ ] **确认保存使用预览数据**
  - 勾选部分货柜
  - 点击"确认保存"
  - 查看数据库，确认保存的是预览数据

- [ ] **资源可用性检查生效**
  - 模拟仓库满的情况
  - 尝试保存
  - 确认应被拒绝

- [ ] **错误处理正确**
  - 传入无效的 previewResults
  - 确认应返回错误

#### 任务 2: 兼容性测试

- [ ] **新版前端（传 previewResults）**
  - 确认正常工作
  - 确认保存速度提升

- [ ] **旧版前端（不传 previewResults）**
  - 确认仍能工作（降级到重新计算）
  - 确认无报错

#### 任务 3: 性能测试

- [ ] **保存速度对比**
  - 优化前：~2-3 秒
  - 优化后：~0.5 秒
  - 确认提升明显

- [ ] **并发场景**
  - 多用户同时保存
  - 确认资源不超卖

---

## 四、预期收益

### 4.1 用户体验提升

| 指标             | 优化前    | 优化后      | 提升     |
| ---------------- | --------- | ----------- | -------- |
| **所见即所得**   | ❌ 不一致 | ✅ 完全一致 | 100%     |
| **用户信任度**   | ⭐⭐      | ⭐⭐⭐⭐⭐  | 显著提升 |
| **操作确定性**   | ❌ 不可控 | ✅ 完全可控 | 100%     |
| **优化按钮响应** | ❌ 无响应 | ✅ 正常响应 | 修复 Bug |

### 4.2 技术收益

| 指标           | 优化前  | 优化后  | 提升     |
| -------------- | ------- | ------- | -------- |
| **计算次数**   | 2 次    | 1 次    | ↓ 50%    |
| **响应时间**   | ~2-3 秒 | ~0.5 秒 | ↑ 4-6 倍 |
| **服务器负载** | 高      | 低      | ↓ 50%    |

### 4.3 开发效率

| 组件                   | 复用场景   | 节省时间         |
| ---------------------- | ---------- | ---------------- |
| OptimizationResultCard | 已复用良好 | -                |
| 内联表格               | 保持现状   | -                |
| **总计**               | -          | **无需额外投入** |

---

## 五、最终结论

### ✅ 方案 B 整体可行

**技术可行性**: ✅ 完全可行  
**组件复用分析**: ✅ 基本正确（部分无需强行复用）  
**风险评估**: ✅ 风险可控  
**实施成本**: ✅ 成本低（约 2-3 天）

### 🎯 推荐实施顺序

1. **Phase 0 (0.5 天)**: 修复"接受优化方案"按钮 Bug（P0 紧急）
2. **Phase 1 (1 天)**: 后端改造（支持 previewResults）
3. **Phase 2 (0.5 天)**: 前端改造（传回 previewResults）
4. **Phase 3 (1 天)**: 测试与验证

### 📋 下一步行动

**立即执行**:

1. ✅ 修复 handleAcceptOptimization 方法
2. ✅ 验证修复效果
3. ✅ 开始 Phase 1 后端改造

**分阶段推进**:

- 按 Phase 1 → Phase 2 → Phase 3 顺序执行
- 每个阶段完成后进行代码审查
- 执行 Linter 检查确保代码质量

---

**评审人**: LogiX Development Team  
**评审结论**: ✅ 通过，建议立即实施  
**版本**: v1.0  
**完成日期**: 2026-03-27
