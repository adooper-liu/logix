# 排产确认保存修复总结

## 修复时间
2026-03-27

## 问题清单

### 问题 1: 提柜日期字段映射错误 ✅
**文件**: `backend/src/controllers/scheduling.controller.ts` 第 2432 行

**错误代码**:
```typescript
trucking.pickupDate = new Date(plannedData.plannedPickupDate);
trucking.returnDate = new Date(plannedData.plannedReturnDate);
```

**修复后**:
```typescript
trucking.plannedPickupDate = new Date(plannedData.plannedPickupDate);
trucking.plannedReturnDate = new Date(plannedData.plannedReturnDate);
```

**影响**: 
- `process_trucking_transport.planned_pickup_date` 为 NULL
- `process_trucking_transport.pickup_date` 包含计划日期
- 前端 `pla` 标签显示为空

---

### 问题 2: 还箱日期未保存 ✅
**文件**: `backend/src/controllers/scheduling.controller.ts` 

**问题**: `savePlannedDates` 函数缺少保存 `EmptyReturn` 表的逻辑

**修复内容**:
1. 添加 `EmptyReturn` 实体导入
2. 在 `savePlannedDates` 函数中添加第 3 步：保存还空箱记录

**修复后代码**:
```typescript
// 3. 保存还空箱 (EmptyReturn)
let emptyReturn = await manager.findOne(EmptyReturn, {
  where: { containerNumber }
});

if (!emptyReturn) {
  emptyReturn = new EmptyReturn();
  emptyReturn.containerNumber = containerNumber;
}

emptyReturn.plannedReturnDate = new Date(plannedData.plannedReturnDate);

await manager.save(emptyReturn);
```

**影响**:
- `process_empty_return.planned_return_date` 为 NULL
- 前端计划还箱日显示 `-`

---

## 修复后的完整代码

### 导入部分
```typescript
import { Request, Response } from 'express';
import { AppDataSource } from '../database';
import { Container } from '../entities/Container';
import { EmptyReturn } from '../entities/EmptyReturn';  // ✅ 新增
import { ExtTruckingSlotOccupancy } from '../entities/ExtTruckingSlotOccupancy';
import { ExtWarehouseDailyOccupancy } from '../entities/ExtWarehouseDailyOccupancy';
import { PortOperation } from '../entities/PortOperation';
import { TruckingCompany } from '../entities/TruckingCompany';
import { TruckingTransport } from '../entities/TruckingTransport';
import { Warehouse } from '../entities/Warehouse';
import { WarehouseOperation } from '../entities/WarehouseOperation';
import { Yard } from '../entities/Yard';
```

### savePlannedDates 函数
```typescript
private async savePlannedDates(plannedData: any, manager: any): Promise<void> {
  const { containerNumber, warehouseId, truckingCompanyId } = plannedData;

  // 1. 保存仓库操作 (WarehouseOperation)
  let warehouseOp = await manager.findOne(WarehouseOperation, {
    where: { containerNumber }
  });

  if (!warehouseOp) {
    warehouseOp = new WarehouseOperation();
    warehouseOp.containerNumber = containerNumber;
  }

  warehouseOp.warehouseId = warehouseId;
  warehouseOp.plannedUnloadDate = new Date(plannedData.plannedUnloadDate);
  warehouseOp.unloadModeActual = plannedData.unloadMode;

  await manager.save(warehouseOp);

  // 2. 保存车队运输 (TruckingTransport)
  let trucking = await manager.findOne(TruckingTransport, {
    where: { containerNumber }
  });

  if (!trucking) {
    trucking = new TruckingTransport();
    trucking.containerNumber = containerNumber;
  }

  trucking.truckingCompanyId = truckingCompanyId;
  trucking.plannedPickupDate = new Date(plannedData.plannedPickupDate);  // ✅ 修复
  trucking.plannedReturnDate = new Date(plannedData.plannedReturnDate);  // ✅ 修复

  await manager.save(trucking);

  // 3. 保存还空箱 (EmptyReturn) - ✅ 新增
  let emptyReturn = await manager.findOne(EmptyReturn, {
    where: { containerNumber }
  });

  if (!emptyReturn) {
    emptyReturn = new EmptyReturn();
    emptyReturn.containerNumber = containerNumber;
  }

  emptyReturn.plannedReturnDate = new Date(plannedData.plannedReturnDate);

  await manager.save(emptyReturn);
}
```

---

## 数据迁移脚本

### 提柜日期修复
**文件**: `backend/scripts/fix_scheduling_date_mapping.sql`

```sql
-- 将 pickup_date 中的计划日期迁移到 planned_pickup_date
UPDATE process_trucking_transport
SET 
  planned_pickup_date = pickup_date,
  pickup_date = NULL,
  pickup_date_source = 'manual'
WHERE 
  planned_pickup_date IS NULL 
  AND pickup_date IS NOT NULL;
```

### 还箱日期修复
还箱日期无需迁移，因为原数据为空，重新排产即可。

---

## 验证步骤

### 1. 重启后端服务
```bash
docker restart logix-backend
```

### 2. 清除浏览器缓存
- 强制刷新：`Ctrl + Shift + R` (Windows) 或 `Cmd + Shift + R` (Mac)

### 3. 执行排产确认保存
1. 打开 Shipment 列表
2. 选择未排产的货柜
3. 点击"智能排产"
4. 查看预览数据
5. 点击"确认保存"

### 4. 检查数据库
```sql
-- 检查提柜日期
SELECT 
  container_number,
  planned_pickup_date AS "计划提柜日",
  pickup_date AS "实际提柜日",
  pickup_date_source AS "数据来源"
FROM process_trucking_transport
ORDER BY container_number DESC
LIMIT 10;

-- 检查还箱日期
SELECT 
  container_number,
  planned_return_date AS "计划还箱日",
  return_time AS "实际还箱日"
FROM process_empty_return
ORDER BY container_number DESC
LIMIT 10;
```

### 5. 检查前端显示
在 Shipment 表格中应该看到：

**提柜日期列**:
- `lfd` - 最晚提柜日 (灰色)
- `pla` - 计划提柜日 (红色) ✅
- `act` - 实际提柜日 (灰色)

**还箱日期列**:
- `lrd` - 最晚还箱日 (灰色)
- `pla` - 计划还箱日 (红色) ✅
- `act` - 实际还箱日 (灰色)

---

## 字段映射规范

| 业务含义 | 数据库表 | 数据库字段 | 实体字段 | 前端标签 |
|---------|---------|-----------|---------|---------|
| 计划提柜日 | process_trucking_transport | planned_pickup_date | plannedPickupDate | pla (提柜) |
| 实际提柜日 | process_trucking_transport | pickup_date | pickupDate | act (提柜) |
| 计划还箱日 | process_empty_return | planned_return_date | plannedReturnDate | pla (还箱) |
| 实际还箱日 | process_empty_return | return_time | returnTime | act (还箱) |
| 最晚提柜日 | process_port_operations | last_free_date | lastFreeDate | lfd |
| 最晚还箱日 | process_empty_return | last_return_date | lastReturnDate | lrd |

---

## 注意事项

1. **执行顺序**: 
   - ✅ 先部署代码修复
   - ✅ 再执行数据迁移
   - ✅ 最后验证前端显示

2. **数据备份**: 
   - 执行数据迁移前务必备份
   - 建议在测试环境先验证

3. **前端刷新**:
   - 代码修复后需要强制刷新浏览器
   - 清除缓存以加载新代码

4. **排产重做**:
   - 已排产的货柜需要重新排产才能更新还箱日期
   - 可以使用"重新排产"功能

---

## 相关文件

- **修复文件**: `backend/src/controllers/scheduling.controller.ts`
- **迁移脚本**: `backend/scripts/fix_scheduling_date_mapping.sql`
- **实体定义**: 
  - `backend/src/entities/TruckingTransport.ts`
  - `backend/src/entities/EmptyReturn.ts`
- **前端显示**: `frontend/src/views/Shipments/Shipments.vue`

---

## 修复完成检查清单

- [x] 修复提柜日期字段映射（pickupDate → plannedPickupDate）
- [x] 修复还箱日期字段映射（returnDate → plannedReturnDate）
- [x] 添加 EmptyReturn 实体导入
- [x] 在 savePlannedDates 中添加保存还空箱逻辑
- [x] 创建数据迁移脚本
- [x] 创建修复说明文档
- [ ] 重启后端服务
- [ ] 执行数据迁移
- [ ] 前端验证显示
- [ ] 更新相关文档

---

## 经验教训

1. **保存数据前必须确认实体字段**
   - 不能假设字段存在
   - 必须先查阅实体定义

2. **完整的业务流程需要保存所有相关表**
   - 排产涉及 3 个表：WarehouseOperation、TruckingTransport、EmptyReturn
   - 不能遗漏任何一个

3. **前端显示问题可能是后端数据缺失**
   - 前端显示 `-` 说明数据为空
   - 需要检查后端是否保存了数据

4. **数据库截图是诊断问题的有力工具**
   - 直接查看表数据可以快速定位问题
   - 比查看代码更直观
