# 还箱日计算算法修复 - 车队还箱能力约束

## 🚨 问题分析

### 原有逻辑的缺陷

**固定公式**：

```typescript
// ❌ 过于简化
if (unloadMode === "Drop off") {
  returnDate.setDate(returnDate.getDate() + 1); // 卸柜后次日还箱
} else {
  returnDate = unloadDate; // Live load 同日还箱
}
```

**问题**：

1. **没有考虑车队还箱能力**：车队每日还箱数量有限制（`daily_return_capacity`）
2. **可能导致排产失败**：如果车队当日还箱能力已满，仍然安排还箱会导致实际无法执行
3. **缺乏灵活性**：无法根据车队的实际能力调整还箱日期

---

## ✅ 新算法设计

### 业务规则

#### ① Drop off 模式（有堆场）

```
业务逻辑：提 < 送 = 卸 ≤ 还

还箱日计算规则：
1. 基础规则：卸柜日 + 1 天（卸柜后次日还箱）
2. 能力约束：检查车队在"卸柜日 +1"的还箱能力
   - 如果有能力 → 当天还
   - 如果能力不足 → 顺延到最近有能力的日期
3. 最终红线：不能超过最晚还箱日（lastReturnDate）
4. 查找范围：最多向前查找 14 天
```

#### ② Live load 模式（无堆场）

```
业务逻辑：提 = 送 = 卸 = 还

还箱日计算规则：
1. 强制规则：还箱日 = 卸柜日（同日）
2. 能力约束：检查车队在卸柜日的还箱能力
   - 如果有能力 → 当天还
   - 如果能力不足 → 需要调整卸柜日（同时调整送仓日）
3. 最终红线：不能超过最晚还箱日
4. 查找范围：最多向前查找 14 天
```

---

## 🔧 核心算法实现

### 1. 还箱日计算方法

```typescript
/**
 * 计算计划还箱日（必须 提<=送<=卸<=还）
 *
 * 业务规则：
 * ① Drop off 模式：还 = 卸 + 1，但受车队还箱能力约束，若能力不足则顺延，最晚不超过 lastReturnDate
 * ② Live load 模式：还 = 卸（同日），若能力不足需调整卸柜日或选择其他车队
 *
 * @param unloadDate 卸柜日
 * @param unloadMode 卸柜方式（Drop off / Live load）
 * @param truckingCompanyId 车队 ID（用于查询还箱能力）
 * @param lastReturnDate 最晚还箱日（最终红线）
 * @param plannedPickupDate 计划提柜日（用于 Live load 模式下调整卸柜日）
 * @returns {
 *   returnDate: 还箱日，
 *   adjustedUnloadDate: 调整后的卸柜日（Live load 模式下可能需要调整）
 * }
 */
private async calculatePlannedReturnDate(
  unloadDate: Date,
  unloadMode: string,
  truckingCompanyId: string,
  lastReturnDate?: Date,
  plannedPickupDate?: Date
): Promise<{
  returnDate: Date;
  adjustedUnloadDate?: Date;
}> {
  const returnDateOnly = new Date(unloadDate);
  returnDateOnly.setUTCHours(0, 0, 0, 0);

  let adjustedUnloadDate: Date | undefined;

  if (unloadMode === 'Live load') {
    // Live load 模式：还 = 卸（同日）
    // 需要检查车队当日的还箱能力
    const availableDate = await this.findEarliestAvailableReturnDate(
      truckingCompanyId,
      returnDateOnly,
      lastReturnDate
    );

    if (!availableDate) {
      // 车队还箱能力不足，且无法在 lastReturnDate 前找到可用日期
      // 返回卸柜日当天（由上层逻辑决定是否更换车队）
      return {
        returnDate: returnDateOnly,
        adjustedUnloadDate: undefined
      };
    }

    // 如果找到的日期不是卸柜日当天，需要调整卸柜日
    if (availableDate.getTime() !== returnDateOnly.getTime()) {
      adjustedUnloadDate = availableDate;
    }

    return {
      returnDate: availableDate,
      adjustedUnloadDate
    };
  } else {
    // Drop off 模式：还 = 卸 + 1，但受车队还箱能力约束
    // 从卸柜日次日起查找最近有还箱能力的日期
    const nextDay = new Date(returnDateOnly);
    nextDay.setUTCDate(nextDay.getUTCDate() + 1);

    const availableDate = await this.findEarliestAvailableReturnDate(
      truckingCompanyId,
      nextDay,
      lastReturnDate
    );

    if (!availableDate) {
      // 车队还箱能力不足，返回卸柜日次日（由上层逻辑决定是否更换车队）
      return {
        returnDate: nextDay,
        adjustedUnloadDate: undefined
      };
    }

    return {
      returnDate: availableDate,
      adjustedUnloadDate: undefined
    };
  }
}
```

### 2. 查找最早可用还箱日

```typescript
/**
 * 查找车队从 earliestDate 起首个有还箱能力的日期
 * @param truckingCompanyId 车队 ID
 * @param earliestDate 起始日期
 * @param lastReturnDate 最晚还箱日（可选，若指定则不能超过此日期）
 * @returns 最早可用的还箱日，若找不到则返回 null
 */
private async findEarliestAvailableReturnDate(
  truckingCompanyId: string,
  earliestDate: Date,
  lastReturnDate?: Date
): Promise<Date | null> {
  // 向前查找最多 14 天（或到 lastReturnDate）
  const maxDaysToSearch = lastReturnDate
    ? Math.min(14, Math.ceil((lastReturnDate.getTime() - earliestDate.getTime()) / (1000 * 60 * 60 * 24)))
    : 14;

  for (let i = 0; i <= maxDaysToSearch; i++) {
    const date = new Date(earliestDate);
    date.setUTCDate(date.getUTCDate() + i);
    date.setUTCHours(0, 0, 0, 0);

    // 如果超过最晚还箱日，停止查找
    if (lastReturnDate) {
      const lastReturn = new Date(lastReturnDate);
      lastReturn.setUTCHours(0, 0, 0, 0);
      if (date > lastReturn) {
        return null;
      }
    }

    // 查询车队当日的还箱档期占用
    const occupancy = await AppDataSource.getRepository(ExtTruckingReturnSlotOccupancy).findOne({
      where: {
        truckingCompanyId,
        slotDate: date
      }
    });

    if (!occupancy) {
      // 无占用记录，使用车队默认还箱能力
      const trucking = await AppDataSource.getRepository(TruckingCompany).findOne({
        where: { companyCode: truckingCompanyId },
        select: ['dailyReturnCapacity', 'dailyCapacity']
      });
      const capacity = trucking?.dailyReturnCapacity ?? trucking?.dailyCapacity ?? 20;
      if (capacity > 0) {
        return date;
      }
    } else if (occupancy.plannedCount < occupancy.capacity) {
      // 有剩余能力
      return date;
    }
  }

  // 找不到可用日期
  return null;
}
```

---

## 📊 算法效果对比

### 场景 1：Drop off 模式 - 车队还箱能力充足

```
输入：
  卸柜日 = 2026-03-27
  卸柜方式 = Drop off
  车队还箱能力 = 20 柜/日
  已占用 = 15 柜
  最晚还箱日 = 2026-04-05

计算过程：
  1. 基础还箱日 = 卸柜日 + 1 = 2026-03-28
  2. 检查 2026-03-28 车队能力：
     - 已占用 15 柜 < 总能力 20 柜 ✅
     - 剩余能力 5 柜 ✅
  3. 找到可用日期

输出：
  还箱日 = 2026-03-28 ✅
  调整卸柜日 = 无
```

### 场景 2：Drop off 模式 - 车队还箱能力不足

```
输入：
  卸柜日 = 2026-03-27
  卸柜方式 = Drop off
  车队还箱能力 = 20 柜/日
  2026-03-28 已占用 = 20 柜（已满）
  2026-03-29 已占用 = 18 柜
  最晚还箱日 = 2026-04-05

计算过程：
  1. 基础还箱日 = 卸柜日 + 1 = 2026-03-28
  2. 检查 2026-03-28 车队能力：
     - 已占用 20 柜 = 总能力 20 柜 ❌ 无能力
  3. 顺延到 2026-03-29
  4. 检查 2026-03-29 车队能力：
     - 已占用 18 柜 < 总能力 20 柜 ✅
     - 剩余能力 2 柜 ✅
  5. 找到可用日期

输出：
  还箱日 = 2026-03-29 ✅（顺延 1 天）
  调整卸柜日 = 无
```

### 场景 3：Live load 模式 - 车队还箱能力不足

```
输入：
  卸柜日 = 2026-03-27
  卸柜方式 = Live load
  车队还箱能力 = 20 柜/日
  2026-03-27 已占用 = 20 柜（已满）
  2026-03-28 已占用 = 15 柜
  最晚还箱日 = 2026-04-05

计算过程：
  1. 基础还箱日 = 卸柜日 = 2026-03-27
  2. 检查 2026-03-27 车队能力：
     - 已占用 20 柜 = 总能力 20 柜 ❌ 无能力
  3. 顺延到 2026-03-28
  4. 检查 2026-03-28 车队能力：
     - 已占用 15 柜 < 总能力 20 柜 ✅
     - 剩余能力 5 柜 ✅
  5. 找到可用日期，需要调整卸柜日

输出：
  还箱日 = 2026-03-28 ✅
  调整卸柜日 = 2026-03-28 ✅（从 3-27 调整到 3-28）
  同时调整送仓日 = 2026-03-28（送 = 卸）
```

### 场景 4：超过最晚还箱日

```
输入：
  卸柜日 = 2026-03-27
  卸柜方式 = Drop off
  车队还箱能力 = 5 柜/日
  2026-03-28 ~ 2026-04-05 都已满
  最晚还箱日 = 2026-04-05

计算过程：
  1. 基础还箱日 = 2026-03-28
  2. 检查 2026-03-28 ~ 2026-04-05：
     - 所有日期都已满 ❌
  3. 超过最晚还箱日，停止查找

输出：
  还箱日 = 2026-03-28（基础日期）
  调整卸柜日 = 无
  ⚠️ 警告：车队还箱能力不足，可能产生滞箱费
```

---

## 🔧 集成到排产流程

### 调用位置

`intelligentScheduling.service.ts` L532-554

```typescript
// 8. 计算计划还箱日（从 EmptyReturn 表获取最晚还箱日）
let lastReturnDate: Date | undefined;
const emptyReturn = await this.emptyReturnRepo.findOne({
  where: { containerNumber: container.containerNumber },
});
if (emptyReturn?.lastReturnDate) {
  lastReturnDate = new Date(emptyReturn.lastReturnDate);
} else if (destPo.lastFreeDate) {
  //  fallback: 从 lastFreeDate + 免费用箱天数计算（默认 7 天）
  lastReturnDate = new Date(destPo.lastFreeDate);
  lastReturnDate.setDate(lastReturnDate.getDate() + 7);
}

// ✅ 新的还箱日计算逻辑：考虑车队还箱能力
const returnDateResult = await this.calculatePlannedReturnDate(unloadDate, unloadMode, truckingCompany.companyCode, lastReturnDate, plannedPickupDate);

const plannedReturnDate = returnDateResult.returnDate;

// 如果卸柜日需要调整（Live load 模式下还箱能力不足）
if (returnDateResult.adjustedUnloadDate) {
  unloadDate = returnDateResult.adjustedUnloadDate;
  // 同时调整送仓日（送 = 卸）
  plannedDeliveryDate = new Date(unloadDate);
  logger.info(
    `[IntelligentScheduling] Adjusted unload date from ${unloadDate.toISOString()} to ${returnDateResult.adjustedUnloadDate.toISOString()} for ${container.containerNumber} due to return capacity`,
  );
}
```

---

## 📋 数据模型

### 车队还箱档期占用表

```sql
CREATE TABLE ext_trucking_return_slot_occupancy (
  id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  trucking_company_id VARCHAR(50) NOT NULL,  -- 车队 ID
  slot_date DATE NOT NULL,                    -- 还箱日期
  planned_count INTEGER DEFAULT 0,            -- 已计划还箱数量
  capacity INTEGER DEFAULT 0,                 -- 总容量（来自 daily_return_capacity）
  remaining INTEGER DEFAULT 0,                -- 剩余可用容量
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(trucking_company_id, slot_date)
);
```

### 车队配置表

```sql
CREATE TABLE dict_trucking_companies (
  company_code VARCHAR(100) PRIMARY KEY,
  company_name VARCHAR(200) NOT NULL,
  daily_capacity INTEGER DEFAULT 20,         -- 每日送柜能力
  daily_return_capacity INTEGER DEFAULT 20,  -- 每日还箱能力
  has_yard BOOLEAN DEFAULT false,            -- 是否有堆场
  ...
);
```

---

## 🎯 业务价值

### 1. 避免排产失败

- ✅ 考虑车队实际还箱能力
- ✅ 避免因能力不足导致实际无法还箱
- ✅ 提前发现并解决资源冲突

### 2. 智能调整

- ✅ Drop off 模式：自动顺延还箱日
- ✅ Live load 模式：自动调整卸柜日
- ✅ 在能力范围内找到最优解

### 3. 成本控制

- ✅ 避免因还箱能力不足导致的滞箱费
- ✅ 优化车队资源利用
- ✅ 提高排产可执行性

### 4. 可追溯性

- ✅ 记录还箱日期调整原因
- ✅ 便于后续优化和审计
- ✅ 提供决策支持数据

---

## 📚 相关文档

- [跨国时区问题修复 - UTC 纯日期比较.md](./跨国时区问题修复 - UTC 纯日期比较.md)
- [送仓日倒挂问题修复 - UTC 日期计算.md](./送仓日倒挂问题修复 - UTC 日期计算.md)
- [智能排产提柜日计算与校验逻辑.md](./智能排产提柜日计算与校验逻辑.md)

---

## ✅ 修复完成

- ✅ 还箱日计算：考虑车队还箱能力
- ✅ Drop off 模式：自动顺延还箱日
- ✅ Live load 模式：自动调整卸柜日
- ✅ 最晚还箱日：最终红线约束
- ✅ 查找范围：最多 14 天
- ✅ 日志记录：调整原因可追溯

---

**修复时间**：2026-03-26  
**影响范围**：智能排产服务（intelligentScheduling.service.ts）  
**业务价值**：解决车队还箱能力约束，提高排产可执行性，避免滞箱费风险
