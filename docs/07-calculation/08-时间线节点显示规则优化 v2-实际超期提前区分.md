# 时间线节点显示规则优化 v2 - 实际超期/提前区分

## 📋 需求背景

在原有时间线显示规则基础上，进一步细化**计划节点与实际节点对比**的显示逻辑。

### 原有逻辑（v1）

当后续实际节点已发生时，计划节点只显示"历时 X 天"，无法区分：
- 实际提柜比最晚提柜日晚 → **超期提柜**
- 实际提柜比最晚提柜日早 → **提前提柜**

### 新需求（v2）

需要明确区分三种状态：
1. **实际超期**：实际日期 > 计划日期（晚于计划）
2. **实际提前**：实际日期 < 计划日期（早于计划）
3. **按时完成**：实际日期 = 计划日期（当天完成）

---

## ✅ 优化方案

### 显示规则（完善版）

#### **计划与预警节点**（ETA、修正 ETA、最晚提柜、最晚还箱）

```
如果后续实际节点已发生：
  → 计算差值：实际日期 - 计划日期
  → 差值 > 0：显示「实际超期：X 天」（橙色警告）
  → 差值 < 0：显示「实际提前：X 天」（绿色成功）
  → 差值 = 0：显示「按时完成」（蓝色信息）
否则：
  → 日期在未来：显示「倒计时：X 天」（绿色）
  → 日期在过去：显示「已超期：X 天」（红色）
```

#### **实际业务节点**（出运、ATA、卸船、实际提柜、实际还箱）

```
总是显示「历时：X 天」（蓝色）
```

---

## 🎯 示例场景

### 场景 1：最晚提柜 vs 实际提柜

#### 情况 A：实际超期提柜
- **最晚提柜**：2026/03/26
- **实际提柜**：2026/03/28（已发生）
- **差值**：2 天（实际晚于计划）
- **显示**：**实际超期 2 天**（橙色）

#### 情况 B：实际提前提柜
- **最晚提柜**：2026/03/26
- **实际提柜**：2026/03/24（已发生）
- **差值**：-2 天（实际早于计划）
- **显示**：**实际提前 2 天**（绿色）

#### 情况 C：按时提柜
- **最晚提柜**：2026/03/26
- **实际提柜**：2026/03/26（已发生）
- **差值**：0 天（当天完成）
- **显示**：**按时完成**（蓝色）

### 场景 2：ETA vs ATA

#### 情况 A：船舶晚到（超期）
- **ETA**：2026/03/20
- **ATA**：2026/03/22（已发生）
- **差值**：2 天（实际晚于计划）
- **显示**：**实际超期 2 天**（橙色）

#### 情况 B：船舶早到（提前）
- **ETA**：2026/03/20
- **ATA**：2026/03/18（已发生）
- **差值**：-2 天（实际早于计划）
- **显示**：**实际提前 2 天**（绿色）

### 场景 3：实际提柜未发生（预警）

- **最晚提柜**：2026/03/26
- **实际提柜**：无
- **今天**：2026/03/30
- **显示**：**已超期 4 天**（红色）

---

## 🔧 技术实现

### 修改文件 1: `DurationDisplay.vue`

#### 函数 1: `getElapsedText` - 历时文本生成

**修改前**：
```typescript
const getElapsedText = (): string => {
  const date = dateObj.value
  if (!date) return ''

  const nextDate = nextDateObj.value
  if (nextDate) {
    const diffDays = Math.floor((nextDate.getTime() - date.getTime()) / (1000 * 60 * 60 * 24))
    if (diffDays <= 0) return ''
    if (diffDays === 1) return '历时 1 天'
    return `历时${diffDays}天`
  }
  
  // ... 其他逻辑
}
```

**修改后**：
```typescript
const getElapsedText = (): string => {
  const date = dateObj.value
  if (!date) return ''

  const nextDate = nextDateObj.value
  if (nextDate) {
    const diffDays = Math.floor((nextDate.getTime() - date.getTime()) / (1000 * 60 * 60 * 24))
    // ✅ 计划节点 vs 实际节点：区分超期/提前
    if (diffDays > 0) {
      // 实际晚于计划 → 实际超期
      if (diffDays === 1) return '实际超期 1 天'
      return `实际超期${diffDays}天`
    } else if (diffDays < 0) {
      // 实际早于计划 → 实际提前
      const absDiff = Math.abs(diffDays)
      if (absDiff === 1) return '实际提前 1 天'
      return `实际提前${absDiff}天`
    } else {
      // diffDays === 0，当天完成
      return '按时完成'
    }
  }

  // 无 nextDate：使用 prevDate 计算普通历时
  const prevDate = prevDateObj.value
  if (!prevDate) return ''
  const diffDays = Math.floor((date.getTime() - prevDate.getTime()) / (1000 * 60 * 60 * 24))
  if (diffDays <= 0) return ''
  if (diffDays === 1) return '历时 1 天'
  return `历时${diffDays}天`
}
```

#### 函数 2: `colorType` - 颜色判断逻辑

**修改前**：
```typescript
if (type === 'elapsed') {
  return 'info' // 历时统一蓝色
}
```

**修改后**：
```typescript
if (type === 'elapsed') {
  // ✅ 历时：区分实际超期/实际提前
  const planWarningLabels = ['ETA', '修正 ETA', '最晚提柜', '最晚还箱']
  
  // 如果当前是计划节点，且有 nextDate（实际节点），根据差值正负判断颜色
  if (planWarningLabels.includes(props.label || '') && nextDateObj.value) {
    const date = dateObj.value
    const nextDate = nextDateObj.value
    if (!date) return 'info' // 安全检查
    const diffDays = Math.floor((nextDate.getTime() - date.getTime()) / (1000 * 60 * 60 * 24))
    
    if (diffDays > 0) {
      // 实际超期 → 橙色警告
      return 'warning'
    } else if (diffDays < 0) {
      // 实际提前 → 绿色成功
      return 'success'
    } else {
      // 按时完成 → 蓝色信息
      return 'info'
    }
  }
  // 普通历时 → 蓝色信息
  return 'info'
}
```

---

## 📊 显示效果对比

### v1 vs v2 对比

| 场景 | v1 显示 | v2 显示 | 改进点 |
|------|--------|--------|--------|
| 最晚提柜 2026/03/26<br>实际提柜 2026/03/28 | 历时 2 天（蓝色） | **实际超期 2 天**（橙色） | ✅ 明确标识超期风险 |
| 最晚提柜 2026/03/26<br>实际提柜 2026/03/24 | 历时 2 天（蓝色） | **实际提前 2 天**（绿色） | ✅ 明确标识提前完成 |
| 最晚提柜 2026/03/26<br>实际提柜 2026/03/26 | 历时 0 天（蓝色） | **按时完成**（蓝色） | ✅ 明确标识准时 |
| ETA 2026/03/20<br>ATA 2026/03/22 | 历时 2 天（蓝色） | **实际超期 2 天**（橙色） | ✅ 船舶晚到预警 |

### 完整场景示例

```
时间线节点：
1. 出运 → 2. ETA → 3. ATA → 4. 卸船 → 5. 最晚提柜 → 6. 实际提柜 → 7. 最晚还箱

场景：船舶晚到 2 天，但提柜提前 1 天

显示效果：
- 出运：历时 5 天（蓝色）
- ETA: 实际超期 2 天（橙色）← 船舶晚到
- ATA: 历时 3 天（蓝色）
- 卸船：历时 1 天（蓝色）
- 最晚提柜：实际提前 1 天（绿色）← 提柜提前
- 实际提柜：历时 0 天（蓝色）
- 最晚还箱：倒计时 3 天（绿色）
```

---

## 🎨 颜色规范（更新版）

| 显示类型 | 颜色 | 含义 | 使用场景 |
|---------|------|------|----------|
| 实际超期 | 橙色（warning） | 实际晚于计划 | 计划节点 vs 实际节点，差值 > 0 |
| 实际提前 | 绿色（success） | 实际早于计划 | 计划节点 vs 实际节点，差值 < 0 |
| 按时完成 | 蓝色（info） | 实际等于计划 | 计划节点 vs 实际节点，差值 = 0 |
| 历时 | 蓝色（info） | 实际业务节点间隔 | 实际业务节点之间 |
| 倒计时 | 绿色（success） | 未来计划节点 | 计划节点在未来，后续无实际发生 |
| 超期 | 红色（danger） | 过去计划节点 | 计划节点在过去，后续无实际发生 |

---

## ✅ 测试场景清单

### 测试 1：最晚提柜 - 实际超期
- 最晚提柜：2026/03/26
- 实际提柜：2026/03/28
- 预期：**实际超期 2 天**（橙色）

### 测试 2：最晚提柜 - 实际提前
- 最晚提柜：2026/03/26
- 实际提柜：2026/03/24
- 预期：**实际提前 2 天**（绿色）

### 测试 3：最晚提柜 - 按时完成
- 最晚提柜：2026/03/26
- 实际提柜：2026/03/26
- 预期：**按时完成**（蓝色）

### 测试 4：ETA - 船舶晚到
- ETA: 2026/03/20
- ATA: 2026/03/22
- 预期：**实际超期 2 天**（橙色）

### 测试 5：ETA - 船舶早到
- ETA: 2026/03/20
- ATA: 2026/03/18
- 预期：**实际提前 2 天**（绿色）

### 测试 6：实际业务节点
- 实际提柜：2026/03/28
- 预期：**历时 2 天**（蓝色）

### 测试 7：边界值 - 1 天超期
- 最晚提柜：2026/03/26
- 实际提柜：2026/03/27
- 预期：**实际超期 1 天**（橙色）

### 测试 8：边界值 - 1 天提前
- 最晚提柜：2026/03/26
- 实际提柜：2026/03/25
- 预期：**实际提前 1 天**（绿色）

---

## 🔗 相关文件

### 修改的文件
1. `frontend/src/components/common/DurationDisplay.vue`
   - 修改 `getElapsedText` 函数（历时文本生成）
   - 修改 `colorType` 计算属性（颜色判断）

2. `frontend/src/views/shipments/components/KeyDatesTimeline.vue`
   - 修改 `getEffectiveHasNextNode` 函数（已有 v1 修改）

### 相关文档
- `docs/07-calculation/07-时间线节点显示规则优化.md`（v1 基础版本）
- `docs/07-calculation/06-免费天数显示问题修复.md`
- `docs/07-calculation/05-时间线节点显示规则修复.md`

---

## 📝 核心原则（v2）

1. **精细化对比**：计划与实际对比时，明确区分超期/提前/按时
2. **视觉强化**：橙色（超期警告）、绿色（提前成功）、蓝色（按时完成）
3. **业务导向**：帮助用户快速识别哪些环节超期、哪些环节提前
4. **数据驱动**：所有显示基于实际日期差值计算，无主观判断

---

**优化时间**：2026-03-30  
**优化人员**：Lingma  
**文档版本**：v2.0  
**依赖版本**：v1.0（时间线节点显示规则优化）
