# 任务 3.5：前端 UI 开发 - Phase 1 实施方案

**创建日期**: 2026-03-17  
**任务**: 3.5 - 前端 UI 开发（Phase 1）  
**状态**: 🚀 **实施中**  
**遵循 Skill**: logix-development, intelligent-scheduling-mapping

---

## 📋 Phase 1 目标

在现有 `SchedulingVisual.vue` 界面中集成成本优化功能，添加"成本优化"标签页。

---

## 🎯 实施步骤

### Step 1: 新增后端 API（优先级：P0）

#### 1.1 扩展 SchedulingController

**文件**: `backend/src/controllers/scheduling.controller.ts`

**新增方法**:

```typescript
/**
 * POST /api/v1/scheduling/evaluate-cost
 * 评估单个方案的成本
 */
evaluateCost = async (req: Request, res: Response): Promise<void> => {
  try {
    const { containerNumber, option } = req.body;

    if (!containerNumber || !option) {
      res.status(400).json({ 
        success: false, 
        message: '缺少必要参数：containerNumber 和 option' 
      });
      return;
    }

    // 调用成本优化服务评估成本
    const costBreakdown = await costOptimizerService.evaluateTotalCost(option);

    res.json({
      success: true,
      data: {
        containerNumber,
        option,
        costBreakdown
      }
    });
  } catch (error: any) {
    logger.error('[Scheduling] evaluateCost error:', error);
    res.status(500).json({ 
      success: false, 
      message: error.message 
    });
  }
};

/**
 * POST /api/v1/scheduling/compare-options
 * 对比多个方案的成本
 */
compareOptions = async (req: Request, res: Response): Promise<void> => {
  try {
    const { containerNumber, options } = req.body;

    if (!containerNumber || !options || options.length === 0) {
      res.status(400).json({ 
        success: false, 
        message: '缺少必要参数：containerNumber 和 options' 
      });
      return;
    }

    // 并行评估所有方案
    const comparisons = await Promise.all(
      options.map(async (option: UnloadOption) => {
        const costBreakdown = await costOptimizerService.evaluateTotalCost(option);
        return {
          option,
          costBreakdown,
          rank: 0 // 后续根据总成本排名
        };
      })
    );

    // 按总成本排序
    comparisons.sort((a, b) => 
      a.costBreakdown.totalCost - b.costBreakdown.totalCost
    );

    // 更新排名
    comparisons.forEach((item, index) => {
      item.rank = index + 1;
    });

    // 获取推荐方案
    const recommendedOption = comparisons[0];
    const savings = comparisons.length > 1 
      ? comparisons[1].costBreakdown.totalCost - recommendedOption.costBreakdown.totalCost 
      : 0;

    res.json({
      success: true,
      data: {
        containerNumber,
        comparisons,
        recommendedOption: {
          option: recommendedOption.option,
          reason: `总成本最低，比其他方案节省 $${savings.toFixed(2)}`,
          savings
        }
      }
    });
  } catch (error: any) {
    logger.error('[Scheduling] compareOptions error:', error);
    res.status(500).json({ 
      success: false, 
      message: error.message 
    });
  }
};

/**
 * GET /api/v1/scheduling/recommend-option/:containerNumber
 * 获取推荐的最优方案
 */
getRecommendOption = async (req: Request, res: Response): Promise<void> => {
  try {
    const { containerNumber } = req.params;

    // TODO: 实现智能推荐逻辑
    // 暂时返回一个简单的响应
    res.json({
      success: true,
      data: {
        containerNumber,
        message: '推荐方案功能开发中',
        optimalOption: null,
        costBreakdown: null,
        alternatives: [],
        reasoning: ''
      }
    });
  } catch (error: any) {
    logger.error('[Scheduling] getRecommendOption error:', error);
    res.status(500).json({ 
      success: false, 
      message: error.message 
    });
  }
};
```

#### 1.2 注册路由

**文件**: `backend/src/routes/scheduling.routes.ts`

```typescript
// 成本评估
router.post('/evaluate-cost', controller.evaluateCost);
router.post('/compare-options', controller.compareOptions);
router.get('/recommend-option/:id', controller.getRecommendOption);
```

---

### Step 2: 创建前端 Service（优先级：P0）

#### 2.1 新建服务文件

**文件**: `frontend/src/services/costOptimization.ts`

```typescript
/**
 * 成本优化服务
 * Cost Optimization Service
 */

import api from './api'
import type { UnloadOption, CostBreakdown } from '@/types/scheduling'

export interface OptionComparison {
  option: UnloadOption
  costBreakdown: CostBreakdown
  rank: number
}

export interface RecommendationResult {
  option: UnloadOption
  reason: string
  savings: number
}

export interface CompareResponse {
  containerNumber: string
  comparisons: OptionComparison[]
  recommendedOption: RecommendationResult
}

export class CostOptimizationService {
  /**
   * 评估单个方案的成本
   */
  async evaluateCost(containerNumber: string, option: UnloadOption): Promise<{
    success: boolean
    data: {
      containerNumber: string
      option: UnloadOption
      costBreakdown: CostBreakdown
    }
  }> {
    const response = await api.post('/scheduling/evaluate-cost', {
      containerNumber,
      option
    })
    return response.data
  }

  /**
   * 对比多个方案
   */
  async compareOptions(containerNumber: string, options: UnloadOption[]): Promise<{
    success: boolean
    data: CompareResponse
  }> {
    const response = await api.post('/scheduling/compare-options', {
      containerNumber,
      options
    })
    return response.data
  }

  /**
   * 获取推荐方案
   */
  async getRecommendation(containerNumber: string): Promise<{
    success: boolean
    data: {
      containerNumber: string
      optimalOption: UnloadOption | null
      costBreakdown: CostBreakdown | null
      alternatives: UnloadOption[]
      reasoning: string
    }
  }> {
    const response = await api.get(`/scheduling/recommend-option/${containerNumber}`)
    return response.data
  }
}

export const costOptimizationService = new CostOptimizationService()
```

#### 2.2 添加类型定义

**文件**: `frontend/src/types/scheduling.ts`（新建）

```typescript
/**
 * 智能排柜相关类型定义
 */

import type { Warehouse } from './warehouse'
import type { TruckingCompany } from './trucking'

/**
 * 卸柜方案选项
 */
export interface UnloadOption {
  containerNumber: string
  warehouse: Warehouse
  unloadDate: string
  strategy: 'Direct' | 'Drop off' | 'Expedited'
  truckingCompany?: TruckingCompany
  isWithinFreePeriod: boolean
  estimatedDemurrage?: number
  estimatedStorage?: number
  estimatedTransport?: number
  totalCost?: number
}

/**
 * 成本明细
 */
export interface CostBreakdown {
  demurrageCost: number      // 滞港费
  detentionCost: number      // 滞箱费
  storageCost: number        // 堆存费
  transportationCost: number // 运输费
  handlingCost: number       // 操作费（加急费等）
  totalCost: number          // 总成本
}

/**
 * 方案对比结果
 */
export interface OptionComparison {
  option: UnloadOption
  costBreakdown: CostBreakdown
  rank: number
}

/**
 * 推荐方案结果
 */
export interface RecommendationResult {
  option: UnloadOption
  reason: string
  savings: number
}
```

---

### Step 3: 开发核心组件（优先级：P0）

#### 3.1 成本评估面板

**文件**: `frontend/src/views/scheduling/components/CostEvaluationPanel.vue`

```vue
<template>
  <div class="cost-evaluation-panel">
    <el-card title="成本评估">
      <!-- 方案选择 -->
      <div class="panel-section">
        <h4>选择卸柜方案</h4>
        <UnloadOptionSelector 
          v-model="selectedOption" 
          :options="availableOptions"
          @change="handleOptionChange"
        />
      </div>

      <!-- 成本明细 -->
      <div v-if="costBreakdown" class="panel-section">
        <h4>成本明细</h4>
        <CostBreakdownDisplay :data="costBreakdown" />
      </div>

      <!-- 成本可视化 -->
      <div v-if="costBreakdown" class="panel-section">
        <h4>成本构成</h4>
        <CostPieChart :data="costBreakdown" />
      </div>

      <!-- 操作按钮 -->
      <div class="actions">
        <el-button @click="evaluate" :loading="loading">重新评估</el-button>
        <el-button type="primary" @click="select" :disabled="!selectedOption">
          选择此方案
        </el-button>
      </div>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, watch } from 'vue'
import { costOptimizationService } from '@/services/costOptimization'
import type { UnloadOption, CostBreakdown } from '@/types/scheduling'
import UnloadOptionSelector from './UnloadOptionSelector.vue'
import CostBreakdownDisplay from './CostBreakdownDisplay.vue'
import CostPieChart from './CostPieChart.vue'

const props = defineProps<{
  containerNumber: string
  availableOptions: UnloadOption[]
}>()

const emit = defineEmits<{
  optionSelected: [option: UnloadOption]
}>()

const selectedOption = ref<UnloadOption | null>(null)
const costBreakdown = ref<CostBreakdown | null>(null)
const loading = ref(false)

const handleOptionChange = async (option: UnloadOption) => {
  loading.value = true
  try {
    const result = await costOptimizationService.evaluateCost(
      props.containerNumber,
      option
    )
    costBreakdown.value = result.data.costBreakdown
  } finally {
    loading.value = false
  }
}

const evaluate = () => {
  if (selectedOption.value) {
    handleOptionChange(selectedOption.value)
  }
}

const select = () => {
  if (selectedOption.value) {
    emit('optionSelected', selectedOption.value)
  }
}
</script>

<style scoped lang="scss">
.cost-evaluation-panel {
  .panel-section {
    margin-bottom: 20px;

    h4 {
      margin-bottom: 12px;
      color: var(--el-text-color-primary);
      font-size: 14px;
      font-weight: 600;
    }
  }

  .actions {
    display: flex;
    justify-content: flex-end;
    gap: 12px;
    margin-top: 20px;
    padding-top: 20px;
    border-top: 1px solid var(--el-border-color-light);
  }
}
</style>
```

#### 3.2 方案对比表格

**文件**: `frontend/src/views/scheduling/components/OptionComparisonTable.vue`

```vue
<template>
  <div class="option-comparison-table">
    <el-table 
      :data="sortedComparisons" 
      stripe
      border
      highlight-current-row
      size="small"
    >
      <el-table-column type="index" label="排名" width="60" />
      
      <el-table-column prop="option.strategy" label="策略" width="100" />
      <el-table-column 
        prop="option.warehouse.warehouseName" 
        label="仓库" 
        width="150" 
      />
      <el-table-column 
        prop="option.unloadDate" 
        label="卸柜日期" 
        width="110"
        :formatter="formatDate"
      />
      
      <el-table-column label="成本明细" align="center">
        <el-table-column 
          prop="costBreakdown.demurrageCost" 
          label="滞港费" 
          width="90" 
        />
        <el-table-column 
          prop="costBreakdown.storageCost" 
          label="堆存费" 
          width="90" 
        />
        <el-table-column 
          prop="costBreakdown.transportationCost" 
          label="运输费" 
          width="90" 
        />
        <el-table-column 
          prop="costBreakdown.handlingCost" 
          label="操作费" 
          width="90" 
        />
      </el-table-column>
      
      <el-table-column 
        prop="costBreakdown.totalCost" 
        label="总成本" 
        width="120"
        sortable
      >
        <template #default="{ row }">
          <span :class="{ 'highlight': row.rank === 1 }">
            ${{ row.costBreakdown.totalCost.toFixed(2) }}
          </span>
        </template>
      </el-table-column>
      
      <el-table-column label="操作" width="120" fixed="right">
        <template #default="{ row }">
          <el-button 
            type="primary" 
            size="small"
            link
            @click="select(row.option)"
          >
            选择
          </el-button>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import type { UnloadOption, OptionComparison } from '@/types/scheduling'

const props = defineProps<{
  comparisons: OptionComparison[]
}>()

const emit = defineEmits<{
  optionSelected: [option: UnloadOption]
}>()

const sortedComparisons = computed(() => {
  return [...props.comparisons].sort((a, b) => 
    a.costBreakdown.totalCost - b.costBreakdown.totalCost
  )
})

const formatDate = (row: OptionComparison) => {
  const date = new Date(row.option.unloadDate)
  return date.toLocaleDateString('zh-CN')
}

const select = (option: UnloadOption) => {
  emit('optionSelected', option)
}
</script>

<style scoped lang="scss">
.option-comparison-table {
  .highlight {
    color: $success-color;
    font-weight: bold;
    font-size: 15px;
  }
}
</style>
```

---

### Step 4: 集成到现有界面（优先级：P1）

#### 4.1 扩展 SchedulingVisual.vue

在现有的 `SchedulingVisual.vue` 中添加"成本优化"标签页：

```vue
<!-- 在资源配置面板的 el-tabs 中添加新标签 -->
<el-tabs v-model="activeTab">
  <el-tab-pane label="仓库" name="warehouse">
    <!-- 现有仓库代码 -->
  </el-tab-pane>
  <el-tab-pane label="车队" name="trucking">
    <!-- 现车队代码 -->
  </el-tab-pane>
  <el-tab-pane label="堆场" name="yard">
    <!-- 现有堆场代码 -->
  </el-tab-pane>
  <el-tab-pane label="资源占用" name="occupancy">
    <!-- 现有资源占用代码 -->
  </el-tab-pane>
  <el-tab-pane label="资源分析" name="analysis">
    <!-- 现有资源分析代码 -->
  </el-tab-pane>
  
  <!-- 新增：成本优化标签页 -->
  <el-tab-pane label="成本优化" name="cost-optimization">
    <CostOptimizationPanel 
      v-if="scheduleResult?.results?.length > 0"
      :container-number="currentContainerNumber"
      :available-options="scheduleResult.results"
      @option-selected="handleOptionSelected"
    />
    <el-empty v-else description="请先执行排产，然后查看成本优化方案" />
  </el-tab-pane>
</el-tabs>
```

---

## ✅ 验收标准

### 功能验收

- [ ] 能够对单个卸柜方案进行成本评估
- [ ] 能够对比至少 3 个不同方案
- [ ] 成本明细展示完整（滞港费、堆存费、运输费、操作费）
- [ ] 方案对比表格正确显示排名
- [ ] 最优方案高亮显示（绿色）

### 技术验收

- [ ] 遵循 logix-development Skill 规范
- [ ] 遵循 intelligent-scheduling-mapping Skill 规范
- [ ] 使用 SCSS 变量（禁止硬编码色值）
- [ ] 类型定义完整（TypeScript）
- [ ] ESLint 检查通过
- [ ] 无控制台错误

### 性能验收

- [ ] 单次成本评估响应时间 < 500ms
- [ ] 方案对比支持至少 10 个方案
- [ ] 前端组件渲染流畅（无明显卡顿）

---

## 📝 注意事项

### 数据库依赖

1. **配置项依赖**: 成本计算依赖于 `dict_scheduling_config` 表中的配置项
   ```sql
   INSERT INTO dict_scheduling_config (config_key, config_value, description)
   VALUES
     ('transport_base_rate_per_mile', '2.5', '运输基础费率 (USD/英里)'),
     ('transport_direct_multiplier', '1.0', 'Direct 模式倍数'),
     ('transport_dropoff_multiplier', '1.2', 'Drop off 模式倍数'),
     ('transport_expedited_multiplier', '1.5', 'Expedited 模式倍数'),
     ('external_storage_daily_rate', '50', '外部堆存日费率'),
     ('expedited_handling_fee', '50', '加急处理费')
   ON CONFLICT (config_key) DO UPDATE SET
     config_value = EXCLUDED.config_value,
     description = EXCLUDED.description;
   ```

2. **测试环境**: 测试环境中可能没有实际数据，生成方案数量为 0 是预期行为

### 配置依赖

所有费率必须从配置表读取，禁止硬编码：

```typescript
// ✅ 正确做法
const baseRate = await this.getConfigNumber('transport_base_rate_per_mile', 2.5)

// ❌ 错误做法
const baseRate = 2.5
```

---

## 🚀 下一步行动

1. **确认方案**: 本方案确认后，开始实施
2. **实施顺序**: 
   - Step 1: 后端 API（2 小时）
   - Step 2: 前端 Service（1 小时）
   - Step 3: 核心组件（4 小时）
   - Step 4: 集成测试（3 小时）
3. **预计完成**: 1-2 天

---

**文档状态**: 📋 **待评审**  
**创建人**: AI Development Team  
**最后更新**: 2026-03-17
