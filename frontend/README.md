# LogiX 前端应用

基于 Vue 3 + TypeScript 的物流管理系统前端界面。

## 🚀 技术栈

- **框架**: Vue 3.4.0 + Composition API
- **语言**: TypeScript 5.3.0
- **构建工具**: Vite 5.0.10
- **UI组件库**: Element Plus 2.4.4
- **状态管理**: Pinia 2.1.7
- **路由**: Vue Router 4.2.5 (Hash 模式)
- **HTTP客户端**: Axios 1.6.2
- **图表库**: ECharts 5.4.3
- **CSS预处理器**: Sass 1.69.5 (Modern Compiler API)

## 📁 项目结构

```
frontend/
├── public/              # 静态资源
│   └── index.html      # HTML 入口文件
├── src/
│   ├── assets/         # 静态资源
│   │   └── styles/    # 全局样式
│   │       ├── global.scss       # 全局样式重置
│   │       └── variables.scss   # SCSS 变量
│   ├── components/     # 组件
│   │   └── layout/    # 布局组件
│   │       └── Layout.vue       # 主布局（侧边栏 + 头部 + 内容）
│   ├── views/         # 页面组件
│   │   ├── Login.vue           # 登录页
│   │   ├── dashboard/          # 仪表板
│   │   ├── shipments/          # 集装箱管理
│   │   ├── monitoring/         # 监控中心
│   │   └── settings/           # 系统设置
│   ├── router/        # 路由配置
│   │   └── index.ts    # 路由定义和守卫
│   ├── store/         # 状态管理
│   │   └── user.ts     # 用户状态
│   ├── services/      # API 服务
│   │   ├── api.ts      # Axios 配置和拦截器
│   │   └── container.ts # 集装箱 API
│   ├── types/         # TypeScript 类型定义
│   │   └── index.ts    # 通用类型
│   ├── App.vue        # 根组件
│   └── main.ts        # 应用入口
├── index.html         # 项目入口 HTML
├── vite.config.ts     # Vite 配置
├── tsconfig.json      # TypeScript 配置
└── package.json       # 项目依赖和脚本
```

## 🛠️ 开发指南

### 环境要求
- Node.js >= 18.x
- npm >= 9.x 或 pnpm >= 8.x

### 安装依赖
```bash
cd frontend
npm install
```

### 启动开发服务器
```bash
npm run dev
```
访问 `http://localhost:5173/`

### 构建生产版本
```bash
npm run build
```
输出到 `dist/` 目录

### 预览生产构建
```bash
npm run preview
```

### 类型检查
```bash
npm run type-check
```

### 代码格式化
```bash
npm run format
```

## 🔧 配置说明

### Vite 配置 (vite.config.ts)
- **端口**: 5173
- **主机**: 0.0.0.0（支持局域网访问）
- **路由模式**: Hash 模式（`createWebHashHistory()`）
- **API 代理**: `/api` → `http://localhost:3001`
- **Sass**: 使用 Modern Compiler API
- **路径别名**: `@` 指向 `src` 目录

### 路由配置
- 使用 **Hash 模式**避免服务器配置问题
- **路由守卫**: 检查 localStorage 中的 token
- **未认证**: 自动重定向到 `/login`
- **已认证**: 首页重定向到 `/dashboard`
- **404 处理**: 自动重定向到 `/login`

### 环境变量
在项目根目录创建 `.env` 文件：
```bash
VITE_API_BASE_URL=http://localhost:3001/api
```

## 🎯 功能模块

### 1. 登录页 (`/login`)
- 用户名/密码登录
- 演示账号: admin / 123456
- JWT Token 认证（当前为模拟登录）

### 2. 仪表板 (`/dashboard`)
- **统计卡片**: 总集装箱数、在途、异常、已完成
- **状态分布图**: ECharts 环形图显示集装箱状态
- **最近活动**: 显示最近的集装箱操作记录

### 3. 集装箱管理 (`/shipments`)
- 集装箱列表展示
- 搜索和筛选功能
- 状态跟踪和详情查看
- 数据同步功能

### 4. 监控中心 (`/monitoring`)
- 系统服务健康状态
- 性能指标监控
- 实时告警信息
- ECharts 图表展示

### 5. 系统设置 (`/settings`)
- API 连接配置
- 适配器参数设置
- 通知配置管理
- 系统参数配置

## 🔐 认证流程

### 当前实现（模拟登录）
1. 用户输入用户名和密码
2. 调用 `userStore.login()`
3. 生成模拟 Token 并存储到 `localStorage`
4. 跳转到仪表板

### API 认证
- **请求拦截器**: 自动添加 `Authorization: Bearer ${token}`
- **响应拦截器**: 401 自动清除 token 并跳转登录
- **路由守卫**: 检查 token 决定访问权限

## 📱 响应式设计

- **断点**: xs (480px), sm (768px), md (992px), lg (1200px), xl (1920px)
- **布局**: 侧边栏可收缩
- **适配**: 支持桌面端和移动端

## 🎨 样式系统

### SCSS 变量
- 主色: `#409EFF`
- 成功: `#67C23A`
- 警告: `#E6A23C`
- 危险: `#F56C6C`
- 信息: `#909399`

### 全局样式
- 使用 Modern Sass Compiler API
- 统一的间距、字体、阴影系统
- 自定义滚动条样式

## 🐛 调试和开发

### Vite 开发服务器快捷键
- `h + enter`: 显示帮助
- `r + enter`: 重启服务器
- `u + enter`: 显示服务器 URL
- `c + enter`: 清空控制台
- `o + enter`: 在浏览器中打开
- `q + enter`: 退出

### 浏览器开发者工具
- Vue DevTools: 检查组件和状态
- Console: 查看路由守卫日志
- Network: 查看 API 请求

### 路由调试
路由守卫输出详细的日志信息：
- 当前路由路径
- 目标路由路径
- 认证状态
- 跳转决策

## 🚀 部署

### 生产构建
```bash
npm run build
```

### Docker 部署
```dockerfile
FROM node:18-alpine as builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### Nginx 配置 (Hash 模式)
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## 📚 技术文档

- [Vue 3 官方文档](https://vuejs.org/)
- [Element Plus 文档](https://element-plus.org/)
- [Vite 文档](https://vitejs.dev/)
- [Vue Router 4 文档](https://router.vuejs.org/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [ECharts 文档](https://echarts.apache.org/)

## 🔗 相关项目

- **后端**: `../backend`
- **数据库**: TimescaleDB (PostgreSQL)
- **监控系统**: Grafana + Prometheus

## 📝 注意事项

1. **Hash 路由**: 使用 Hash 模式避免服务器配置问题
2. **SCSS**: 使用 Modern Compiler API，不使用 `@import`
3. **认证**: 当前为模拟登录，需要对接后端 API
4. **开发**: Vite 热更新，修改代码自动刷新
5. **代理**: `/api` 请求会被代理到后端服务