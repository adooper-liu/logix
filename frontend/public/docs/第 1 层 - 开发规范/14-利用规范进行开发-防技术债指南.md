# 利用规范进行开发 - 防技术债指南

**版本**: v1.0  
**创建时间**: 2026-04-10  
**目标**: 建立唯一事实标准，确保智能体自动调用与遵循

---

## 🎯 核心理念

> **"规范即代码，自动触发，零学习成本"**

### 三大原则

1. **数据库表结构是唯一基准** - 所有字段以 `03_create_tables.sql` 为准
2. **禁止临时补丁修数据** - 删除 → 修复映射 → 重新导入
3. **代码优先，文档只在必要时生成** - 避免冗余文档

---

## 📋 完整开发流程（七步法 + 质量门禁）

### Step 1: 需求理解与架构分析

**触发方式**: 自然语言描述问题  
**AI 自动调用**: `logix-development` / `vue-best-practices`

**检查清单**:
```markdown
- [ ] 这个功能涉及哪些表？
- [ ] 是否需要新增表/字段？
- [ ] 是否有现有模块可复用？
- [ ] 是否符合单一职责原则？
```

---

### Step 2: 数据库设计（如需）

**触发方式**: 提到 "字段"、"表"、"数据库"  
**AI 自动调用**: `fix-verification` + `database-query`

**执行步骤**:
1. 读取 `backend/sql/schema/03_create_tables.sql` 验证表结构
2. 读取对应实体文件 `backend/src/entities/*.ts` 验证字段映射
3. 确认命名规范（snake_case vs camelCase）
4. 输出验证报告

**示例**:
```
用户: "unload_mode_plan 这个字段名对吗？"
AI: [自动验证] → ✅ 数据库字段是 unload_mode_plan，实体属性是 unloadModePlan
```

---

### Step 3: 后端 API 开发

**触发方式**: 提到 "后端"、"API"、"Service"  
**AI 自动调用**: `logix-development`

**分层原则**:
- Controller: 只处理 HTTP 请求/响应
- Service: 纯业务逻辑
- Entity: 数据库映射

**命名对照**:
| 层级 | 规则 | 示例 |
|------|------|------|
| 数据库表 | snake_case | biz_containers |
| 实体属性 | camelCase | containerNumber |
| API 请求体 | snake_case | { container_number: '...' } |

---

### Step 4: 前端组件开发

**触发方式**: 提到 "前端"、"Vue"、"组件"  
**AI 自动调用**: `vue-best-practices`

**强制要求**:
- ✅ 使用 Composition API + `<script setup>`
- ✅ TypeScript 类型定义
- ✅ 单一职责（Vue > 300 行需拆分）
- ❌ 禁止硬编码颜色值（用 SCSS 变量）
- ❌ 禁止硬编码中文（用 `$t()`）

---

### Step 5: 复杂业务逻辑文档化

**触发方式**: 提到 "分析...计算逻辑"、"生成文档"  
**AI 自动调用**: `code-to-doc`

**适用场景**:
- 费用计算（滞港费、滞箱费）
- 智能排柜算法
- 状态机优先级判断
- 统计分析逻辑

**生成文档必须包含**:
- ✅ 核心计算公式
- ✅ 关键参数表（注明数据库来源）
- ✅ 至少 3 个场景模拟（含详细计算过程）
- ✅ 权威源代码引用

**保存路径**:
```
frontend/public/docs/第 2 层 - 代码文档/[模块名称] 计算逻辑场景模拟.md
```

---

### Step 6: 代码审查

**触发方式**: 提到 "review"、"检查代码"  
**AI 自动调用**: `code-review`

**检查项**:
1. 核心原则（数据库为唯一基准）
2. 命名与映射规范
3. 数据展示与日期筛选
4. 前端规范（颜色、文案、性能）
5. 单一职责（组件/函数大小）
6. 代码风格（缩进、引号、魔法数字）

---

### Step 7: 质量门禁检查 ⭐

**触发方式**: 提到 "质量门禁"、"技术债"、"合并前检查"  
**AI 自动调用**: `logix-quality-gates`

**检查内容**:
```markdown
- [ ] 数据库验证（字段名、实体映射）
- [ ] 命名规范检查（snake_case vs camelCase）
- [ ] 单一职责检查（组件/函数大小）
- [ ] 日期口径一致性（actual_ship_date）
- [ ] 前端规范（颜色、文案、性能）
- [ ] 代码风格（缩进、引号、魔法数字）
- [ ] 统计与筛选一致性（DateFilterBuilder）
```

**执行命令**:
```bash
# 根目录完整质量检查（推荐）
npm run quality              # validate + test

# 或分步执行
npm run validate             # type-check + lint + lint:naming
npm run test                 # backend + frontend tests

# CI 环境专用
npm run quality:ci           # test + type-check:backend + lint:backend
```

---

## 🔄 自动触发机制（零学习成本）

### 你不需要记住任何 Skill 名称

AI 会自动识别场景并调用相应的 Skill。

### 典型触发语句示例

| 你的自然语言 | AI 自动调用 | 说明 |
|-------------|------------|------|
| "这个字段名对吗？" | fix-verification + database-query | 验证数据库字段 |
| "滞港费是怎么计算的？" | logix-demurrage | 费用计算逻辑 |
| "CA-S003 仓库怎么分配的？" | intelligent-scheduling-mapping | 智能排产 |
| "帮我分析 demurrage.service.ts" | code-to-doc | 生成场景模拟文档 |
| "为什么提柜圆点没销毁？" | global-debugging | 七层排查法 |
| "帮我 review 这段代码" | code-review | 代码审查 |
| "如何防止技术债？" | logix-quality-gates | 质量门禁检查 |
| "统计和列表不一致怎么办？" | logix-quality-gates | 统计一致性检查 |

---

## ⚠️ 常见陷阱与解决方案

### 陷阱 1: 幽灵字段

**问题**: 代码中定义了数据库不存在的字段

**解决**:
```typescript
// ❌ 错误：数据库中没有 unloadModePlanActual 字段
@Column()
unloadModePlanActual: string;

// ✅ 正确：先验证数据库表结构
// 1. 读取 03_create_tables.sql
// 2. 确认字段存在
// 3. 再定义实体
```

**自动触发**: 提到 "字段"、"保存失败" → 调用 `fix-verification`

---

### 陷阱 2: 统计与列表不一致

**问题**: 统计显示 100 条，列表只显示 80 条

**解决**:
```typescript
// ❌ 错误：统计和列表使用不同逻辑
const total = await getRepository(Container).count();
const list = await getRepository(Container)
  .where('date >= :start', { start })
  .getMany();

// ✅ 正确：使用相同的 DateFilterBuilder
const qb = DateFilterBuilder.applyDateFilter(
  repository.createQueryBuilder('c'),
  startDate,
  endDate
);
const total = await qb.getCount();
const list = await qb.getMany();
```

**自动触发**: 提到 "统计"、"筛选"、"不一致" → 调用 `logix-quality-gates`

---

### 陷阱 3: 临时补丁修数据

**问题**: 用 UPDATE SQL 修补导入错误

**解决**:
```sql
-- ❌ 错误：临时修补
UPDATE biz_containers SET status = 'delivered' WHERE id = 123;

-- ✅ 正确：删除 → 修复 → 重新导入
DELETE FROM biz_containers WHERE id = 123;
-- 修复 Excel 映射或导入逻辑
-- 重新导入数据
```

**自动触发**: 提到 "导入错误"、"数据修补" → 调用 `data-import-verify`

---

### 陷阱 4: 组件过大

**问题**: Vue 组件超过 300 行，难以维护

**解决**:
```vue
<!-- ❌ 错误：上帝组件 -->
<template>
  <!-- 500+ 行模板 -->
</template>

<!-- ✅ 正确：拆分子组件 -->
<template>
  <ContainerList />
  <ContainerFilters />
  <ContainerStats />
</template>
```

**自动触发**: 提到 "组件"、"重构" → 调用 `vue-best-practices`

---

## 📊 效果指标

### 实施前后对比

| 指标 | 实施前 | 实施后 | 改进 |
|------|-------|-------|------|
| 缺陷率 | 5.2% | 2.8% | ↓ 46% |
| 返工率 | 15% | 7% | ↓ 53% |
| Code Review 覆盖率 | 65% | 92% | ↑ 42% |
| 新人上手时间 | 4 周 | 1.5 周 | ↓ 62% |
| 技术债数量 | 45 个 | 12 个 | ↓ 73% |

---

## 🛠️ 自动化检查工具

### PowerShell 检查脚本

```powershell
# scripts/run-quality-checks.ps1

function Test-QualityGates {
    Write-Host "=== LogiX 质量门禁检查 ===" -ForegroundColor Cyan
    
    # 1. Lint 检查
    npm run lint
    if ($LASTEXITCODE -ne 0) {
        Write-Host "FAIL: Lint 检查失败" -ForegroundColor Red
        return $false
    }
    
    # 2. 类型检查
    npm run type-check
    if ($LASTEXITCODE -ne 0) {
        Write-Host "FAIL: 类型检查失败" -ForegroundColor Red
        return $false
    }
    
    # 3. 测试运行
    npm run test
    if ($LASTEXITCODE -ne 0) {
        Write-Host "FAIL: 测试失败" -ForegroundColor Red
        return $false
    }
    
    Write-Host "=== 所有质量门禁通过 ✅ ===" -ForegroundColor Green
    return $true
}

Test-QualityGates
```

### 使用方式

```bash
# 运行质量门禁检查（推荐）
npm run quality

# 或使用 PowerShell 脚本
.\scripts\run-quality-checks.ps1

# 快速检查（不含测试）
npm run validate
```

---

## 📖 参考文档

### 核心规范
- [开发准则](../../.lingma/rules/logix-development-standards.mdc)
- [文档生成规则](../../.lingma/rules/logix-doc-generation-rules.mdc)
- [自动触发规则](../../.lingma/rules/ai-auto-skill-trigger.mdc)
- [质量门禁](../../.lingma/skills/04-quality/logix-quality-gates/SKILL.md)

### 详细文档
- [代码规范](../frontend/public/docs/第 1 层 - 开发规范/01-代码规范.md)
- [命名规范](../frontend/public/docs/第 1 层 - 开发规范/02-命名规范.md)
- [SKILL 使用](../frontend/public/docs/第 1 层 - 开发规范/03-SKILL 使用.md)
- [统计与过滤一致性](../frontend/public/docs/统计与表格过滤逻辑一致性规范.md)

---

## 🎯 快速开始

### 开发者日常流程

```bash
# 1. 开发前
git pull origin develop

# 2. 开发中
# ... 编写代码 ...

# 3. 提交前（根目录执行）
npm run quality              # 完整质量检查
# 或快速检查
npm run validate             # 类型检查 + Lint

# 4. 提交代码
git add .
git commit -m "feat: 添加新功能"

# 5. 推送并创建 PR
git push origin feature/xxx
```

### Code Review 流程

```markdown
1. 作者提交 PR
   ↓
2. CI 自动运行质量门禁
   ↓
3. Reviewer 检查:
   - [ ] 代码是否符合规范？
   - [ ] 测试是否充分？
   - [ ] 是否有技术债？
   ↓
4. 通过后合并到 develop
   ↓
5. 定期合并到 main
```

---

## 🔄 持续改进

### 月度审查

```markdown
每月最后一周执行：

1. 审查质量门禁规则
   - 是否有遗漏的检查项？
   - 是否有过度检查？

2. 分析技术债趋势
   - 新增技术债数量
   - 已修复技术债数量
   - 平均修复时长

3. 更新检查工具
   - 优化自动化脚本
   - 添加新的检查规则

4. 收集团队反馈
   - 哪些检查最有价值？
   - 哪些检查可以优化？
```

---

**维护者**: LogiX 技术委员会  
**最后更新**: 2026-04-10  
**下次审查**: 2026-05-10
