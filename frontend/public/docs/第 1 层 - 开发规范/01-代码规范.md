# 代码规范

LogiX 项目代码编写规范。

## 核心原则

### 数据库优先

```
数据库表结构是唯一基准
开发顺序：SQL → 实体 → API → 前端 → 联调
禁止反向：代码对齐数据库，不反向改库补数据
```

### 数据完整性

```
禁止临时补丁：不用 UPDATE/INSERT 修补导入错误
正确流程：删除错误数据 → 修复映射/逻辑 → 重新导入
```

### 日期口径统一

```
全项目统一：所有数据展示使用顶部日期范围筛选
后端口径：actual_ship_date（备货单）→ shipment_date（海运）
```

## 命名规则

### 命名对照表

| 层级       | 规则           | 示例                        |
| ---------- | -------------- | --------------------------- |
| 数据库表   | snake_case     | biz_containers              |
| 数据库字段 | snake_case     | container_number            |
| 实体属性   | camelCase      | containerNumber             |
| API 请求   | snake_case     | { container_number: '...' } |
| 前端组件   | PascalCase.vue | ContainerList.vue           |
| 组合式函数 | usePascalCase  | useContainerData            |
| CSS 类名   | kebab-case     | .container-card             |

### 表前缀含义

```
dict_;     # 字典表
biz_;      # 业务表
process_;  # 流程表
ext_;      # 扩展表
```

## 后端规范

### 目录结构

```
src/
├── controllers/    # API 入口，处理请求/响应
├── services/      # 业务逻辑
├── entities/      # 数据库实体
├── routes/        # 路由定义
├── middleware/    # 中间件
├── constants/    # 常量
└── utils/         # 工具函数
```

### 分层原则

- Controller: 处理 HTTP 请求/响应，不包含业务逻辑
- Service: 业务逻辑，不处理 HTTP 相关
- Entity: 数据库映射，字段与表结构一致

### 错误处理

```typescript
// 统一响应格式
{ success: boolean, data?: any, message?: string }

// 错误抛出
throw new Error('错误信息')
// 或
throw new AppError('错误码', '错误信息', HTTP状态码)
```

## 前端规范

### 目录结构

```
src/
├── components/    # 通用组件
├── views/         # 页面组件
├── composables/  # 组合式函数
├── services/     # API 调用
├── store/        # 状态管理
└── router/       # 路由配置
```

### 组件规范

```vue
<script setup lang="ts">
// 1. 类型定义
interface Props {
  title: string
}

// 2. Props 定义
const props = defineProps<Props>()

// 3. 事件
const emit = defineEmits<{
  (e: 'update', value: string): void
}>()
</script>

<template>
  <!-- 模板 -->
</template>

<style scoped>
/* 样式 */
</style>
```

### API 调用

```typescript
// 使用 services 层
import containerService from '@/services/container'

// 调用
const containers = await containerService.getList(params)
```

## 代码质量

### 必须

- 使用 TypeScript 类型定义
- 使用 ESLint 检查代码
- 使用 Prettier 格式化代码
- 组件必须有 props 类型定义

### 禁止

- 禁止使用 any 类型（除非必要）
- 禁止硬编码颜色值（使用 SCSS 变量）
- 禁止直接在组件中写业务逻辑
- 禁止直接操作 DOM

[Atlassian 帐户 | 安全 | API 令牌](https://id.atlassian.com/manage-profile/security/api-tokens)

**注意**: 请勿在代码或文档中硬编码 API Token，应使用环境变量管理。
