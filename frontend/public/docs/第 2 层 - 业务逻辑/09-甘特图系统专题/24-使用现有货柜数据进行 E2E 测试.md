# 使用现有货柜数据进行 E2E 测试

**创建时间**: 2026-04-06  
**数据来源**: 数据库查询结果  
**状态**: ✅ 已找到可用数据

---

## 一、可用的测试货柜

通过数据库查询，找到以下有完整数据的货柜：

| 柜号 | 状态 | 提柜日 | 仓库 | 车队ID |
|------|------|--------|------|--------|
| HMMU6855127 | picked_up | 2026-04-03 | IT-S002 | RT_LOGISTICA_SRL_ |
| HMMU6232153 | at_port | 2026-04-02 | IT-S002 | RT_LOGISTICA_SRL_ |
| HMMU6019657 | at_port | 2026-04-03 | IT-S002 | RT_LOGISTICA_SRL_ |
| KOCU5129260 | picked_up | 2026-04-03 | IT-S002 | RT_LOGISTICA_SRL_ |
| GAOU6195045 | picked_up | 2026-04-03 | IT-S002 | RT_LOGISTICA_SRL_ |

**推荐使用**: `HMMU6232153` (at_port 状态，更适合测试优化)

---

## 二、更新 E2E 测试

### 2.1 修改测试文件

编辑 `e2e/cost-optimization.spec.ts`，将硬编码的日期和选择器改为实际数据。

**修改前**:
```typescript
test('should show optimization panel when dragging pickup date within free period', async ({ page }) => {
  const pickupNode = page.locator('[data-testid="pickup-node"]').first()
  await expect(pickupNode).toBeVisible()

  await pickupNode.dragTo(page.locator('[data-date="2026-04-10"]'))
  // ...
})
```

**修改后**:
```typescript
test('should show optimization panel when dragging pickup date within free period', async ({ page }) => {
  // 使用实际的提柜节点（2026-04-02）
  const pickupNode = page.locator('[data-testid="pickup-node"][data-date="2026-04-02"]').first()
  await expect(pickupNode).toBeVisible()

  // 拖拽到新日期（提前 2 天）
  await pickupNode.dragTo(page.locator('[data-date="2026-03-31"]'))
  
  // 等待优化 API 调用
  await page.waitForResponse(
    (response) =>
      response.url().includes('/scheduling/optimize-container/') &&
      response.status() === 200
  )
  
  // 验证优化面板显示
  const optimizationPanel = page.locator('.cost-optimization-panel')
  await expect(optimizationPanel).toBeVisible({ timeout: 5000 })
})
```

---

### 2.2 关键修改点

#### 修改 1: 定位特定日期的节点

```typescript
// 旧代码
const pickupNode = page.locator('[data-testid="pickup-node"]').first()

// 新代码（使用实际日期）
const pickupNode = page.locator('[data-testid="pickup-node"][data-date="2026-04-02"]').first()
```

#### 修改 2: 拖拽到合理的新日期

```typescript
// 旧代码
await pickupNode.dragTo(page.locator('[data-date="2026-04-10"]'))

// 新代码（提前 2 天，在免费期内）
await pickupNode.dragTo(page.locator('[data-date="2026-03-31"]'))
```

#### 修改 3: 验证 API 响应

确保 API 返回的数据与实际货柜匹配：

```typescript
await page.waitForResponse(async (response) => {
  if (!response.url().includes('/scheduling/optimize-container/')) {
    return false
  }
  
  const data = await response.json()
  return data.success && data.data.containerNumber === 'HMMU6232153'
})
```

---

## 三、验证步骤

### 3.1 手动验证（推荐先执行）

**步骤**:

1. **打开甘特图页面**
   ```
   http://localhost:5173/gantt
   ```

2. **查找货柜 HMMU6232153**
   - 在搜索框输入 `HMMU6232153`
   - 或者滚动找到该货柜

3. **验证 data-testid 属性**
   - 按 F12 打开开发者工具
   - 切换到 Elements 面板
   - 搜索 `data-testid="pickup-node"`
   - 确认存在且 `data-date="2026-04-02"`

4. **手动拖拽测试**
   - 拖拽提柜节点到 2026-03-31
   - 观察 Console 日志
   - 验证优化面板是否显示

5. **记录结果**
   - 如果成功，记录日志
   - 如果失败，记录错误信息

---

### 3.2 运行 E2E 测试

**命令**:
```bash
cd d:\Gihub\logix\frontend
npx playwright test e2e/cost-optimization.spec.ts --reporter=list
```

**预期结果**:
- 至少 1-2 个测试通过
- 其他测试可能因 Mock 数据问题失败

---

## 四、常见问题

### Q1: 找不到 `[data-testid="pickup-node"]` 元素

**原因**: GanttDot 组件可能还没有重新编译

**解决**:
```bash
# 重启前端开发服务器
cd d:\Gihub\logix\frontend
npm run dev
```

---

### Q2: 优化面板不显示

**可能原因**:
1. API 返回 `savings = 0`（无节省空间）
2. API 调用失败
3. 货柜缺少必要数据

**调试方法**:
```javascript
// 在 Console 中查看日志
// 应该看到：
// [triggerCostOptimization] 开始成本优化...
// [triggerCostOptimization] 调用优化 API: {...}
// [triggerCostOptimization] 优化结果: { savings: XXX, ... }
```

---

### Q3: API 返回错误

**检查后端服务**:
```bash
# 查看后端日志
cd d:\Gihub\logix\backend
npm run start:dev
```

**测试 API**:
```bash
curl -X POST http://localhost:3001/api/v1/scheduling/optimize-container/HMMU6232153 \
  -H "Content-Type: application/json" \
  -d '{
    "warehouseCode": "IT-S002",
    "truckingCompanyId": "RT_LOGISTICA_SRL_",
    "basePickupDate": "2026-04-02"
  }'
```

---

## 五、下一步建议

### 方案 A: 先手动验证（推荐）

1. 按照"3.1 手动验证"步骤执行
2. 记录验证结果
3. 如果成功，再运行 E2E 测试
4. 如果失败，根据错误信息调整

**优点**: 
- ✅ 快速发现问题
- ✅ 无需修改测试代码
- ✅ 可以直观看到效果

---

### 方案 B: 直接运行 E2E 测试

1. 更新测试文件中的日期
2. 运行 E2E 测试
3. 根据测试结果调整

**优点**:
- ✅ 自动化程度高
- ✅ 可重复执行

**缺点**:
- ⚠️ 可能需要多次调整
- ⚠️ 调试不如手动直观

---

## 六、总结

### 已完成

✅ **第 1 步**: 添加 data-testid 属性到 GanttDot 组件  
✅ **第 2 步**: 找到可用的测试货柜数据  
⏳ **第 3 步**: 验证后端 API（待执行）

### 下一步

**立即执行** (今天):
1. ⏳ 手动验证优化功能（使用货柜 HMMU6232153）
2. ⏳ 测试后端优化 API
3. ⏳ 根据验证结果更新 E2E 测试

**预计时间**: 30-60分钟

---

**文档状态**: ✅ **已完成**  
**测试数据**: HMMU6232153 (at_port, 提柜日 2026-04-02)  
**下一步**: 手动验证 + API 测试
