# Development Dockerfile
# 用于开发环境，支持热重载

FROM node:18-alpine

# 安装 git (某些 npm 包需要)
RUN apk add --no-cache git

WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装所有依赖 (包括开发依赖)
RUN npm install

# 暴露端口
EXPOSE 3001

# 设置开发环境变量
ENV NODE_ENV=development \
    PORT=3001

# 使用 nodemon 或 tsx watch 支持热重载
CMD ["npm", "run", "dev"]
