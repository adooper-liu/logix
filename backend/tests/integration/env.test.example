NODE_ENV=test
PORT=3001
API_PREFIX=/api/v1

# Database (for integration tests / CI services)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=logix_test
DB_POOL_MIN=1
DB_POOL_MAX=5
DB_SYNCHRONIZE=false

# Redis (for integration tests / CI services)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
